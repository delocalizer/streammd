[tox]
envlist = py310

[testenv]
commands = coverage erase
           coverage run --source=streammd -m unittest discover
           coverage report
           coverage html
deps = coverage
       pysam
       -rrequirements.txt

