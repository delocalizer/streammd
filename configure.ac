# Initialize autoconf
AC_INIT(streammd, 0.1)

# Store auxiliary build files in their own dir
AC_CONFIG_AUX_DIR([build-aux])

# Initialize automake
AM_INIT_AUTOMAKE

# Check for C++ compiler
AC_PROG_CXX

# What to make 
AC_CONFIG_FILES([
		 Makefile
		 src/Makefile])

# Generate the output
AC_OUTPUT
